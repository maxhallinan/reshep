import { shapeFrom, } from './../src';

describe(`reshep > shapeFrom`, function () {
  it(`Throws a TypeError if the first argument is not an object.`, function () {});
  it(`Throws a TypeError if the first argument is not an object of strings.`, function () {});
  it(`Returns a higher-order component.`, function () {});
  it(`Component maps deeply nested props for each entry in \`pathMap\`.`, function () {});
  it(`Component passes new props to base component.`, function () {});
  it(`The shape of the new props matches the shape described by \`pathMap\``, function () {});
});
