import { shapeWith, } from './../src';

describe(`reshep > shapeWith`, function () {
  it(`Throws a TypeError if the first argument is not an object.`, function () {});
  it(`Throws a TypeError if the first argument is not an object of strings.`, () {});
  it(`Returns a higher-order component.`, function () {});
  it(`Component maps deeply nested props for each entry in \`pathMap\`.`, function () {});
  it(`Component merges owner props and mapped props.`, function () {});
  it(`Mapped props overwrite by owner props.`, function () {});
  it(`Component passes merged props to base component.`, function () {});
});

